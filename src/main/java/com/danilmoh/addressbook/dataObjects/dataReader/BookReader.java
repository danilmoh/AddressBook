package com.danilmoh.addressbook.dataObjects.dataReader;

public class BookReader {
    /*
    public static AddressBook readBook(String addressBookPath) {
        AddressBook result;
        try (Scanner scanner = new Scanner(Paths.get(addressBookPath))){
            while(scanner.hasNextLine()) {
                String line = scanner.next();
            }
        } catch (IOException e) {

        }
    }

     */
}
